{
  "name": "ital-dashboard",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "python -m uvicorn backend.main:app --host 127.0.0.1 --port 8080",
    "server": "python -m uvicorn backend.main:app --host 127.0.0.1 --port 8080",
    "test": "python -m pytest -q",
    "validate:data": "node scripts/validar_dados.mjs",
    "validate:forneria": "node scripts/validar_forneria.mjs",
    "validate:filters": "node scripts/validar_filtros.mjs"
  },
  "dependencies": {
    "@vercel/blob": "^2.6.1",
    "html2canvas": "^1.4.1",
    "papaparse": "^5.4.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
